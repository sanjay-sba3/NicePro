# Materialized locally per workspace/user from AWS Secrets Manager at sync time
# (secrets_sync_service.py's _write_auth_credentials_properties). Never commit this file
# with a real value -- see .gitignore and AuthCredentialsResolver.java.
auth.username=
auth.password=
auth.otp.value=
testuser.username=
testuser.password=
testuser.otp=
